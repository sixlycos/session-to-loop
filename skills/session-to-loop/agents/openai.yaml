interface:
  display_name: "SixLoops"
  short_description: "Find your repo's first reusable agent loop"
  default_prompt: "Use $session-to-loop (SixLoops) to find the first loop in this repo worth trying. Reject weak patterns, stay read-only, and return 1-3 Loop Cards with verifier, state, stop condition, and human gate."
